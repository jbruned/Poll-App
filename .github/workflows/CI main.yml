# Script que implementa la CI en la rama de main.
# Información del linting:
# https://github.com/github/super-linter
name: CI main

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  run-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          # Full git history is needed to get a proper list of changed files within `super-linter`
          fetch-depth: 0

      - name: Lint Code Base
        uses: github/super-linter@v4
        env:
          VALIDATE_ALL_CODEBASE: true
          DEFAULT_BRANCH: "main"
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
  test-job:
    runs-on: ubuntu-latest
    needs: run-lint
    steps:
      - name: Test job
        run: echo Trabajo de test ejecutado
